server:
  port: 10001

spring:
  application:
    name: ocean-ldap
  ldap:
    urls:
      - ldap://localhost:389
    base: dc=example,dc=org
    username: cn=admin,dc=example,dc=org
    password: admin


